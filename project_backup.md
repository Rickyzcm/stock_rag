# 创建必要的文件和目录
```
mkdir -p src/{data_pipeline,rag,api,utils}
mkdir -p data/{financials,news,research_reports}
mkdir -p logs
```


# 创建文件
```
touch docker-compose.yml
touch .env
touch requirements.txt
touch Dockerfile
```